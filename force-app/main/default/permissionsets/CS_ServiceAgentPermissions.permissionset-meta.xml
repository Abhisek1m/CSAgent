<?xml version="1.0" encoding="UTF-8"?>
<PermissionSet xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Generated by CSAgent | 2026-04-22 | Service Agent access to CS_SatisfactionScore__c and related fields</description>
    <hasActivationRequired>false</hasActivationRequired>
    <label>Service Agent Permissions</label>

    <!-- Object Permissions: CS_SatisfactionScore__c -->
    <!-- Default: Read + Create + Edit. Delete is false — agents should not delete CSAT records. -->
    <!-- ViewAll and ModifyAll are false — agents see records based on sharing model (OWD). -->
    <objectPermissions>
        <allowCreate>true</allowCreate>
        <allowDelete>false</allowDelete>
        <allowEdit>true</allowEdit>
        <allowRead>true</allowRead>
        <modifyAllRecords>false</modifyAllRecords>
        <object>CS_SatisfactionScore__c</object>
        <viewAllRecords>false</viewAllRecords>
    </objectPermissions>

    <!-- Field Permissions: only non-required fields get FLS blocks. -->
    <!-- CS_Date__c and CS_Case__c are required=true — omitted; Salesforce blocks FLS on required fields. -->
    <!-- CS_Rating__c is required=false (intentionally blank on creation, filled in by agents). -->
    <fieldPermissions>
        <editable>true</editable>
        <field>CS_SatisfactionScore__c.CS_Rating__c</field>
        <readable>true</readable>
    </fieldPermissions>
    <fieldPermissions>
        <editable>true</editable>
        <field>CS_SatisfactionScore__c.CS_CustomerNotes__c</field>
        <readable>true</readable>
    </fieldPermissions>

    <!-- No classAccesses block: this design is fully declarative — no Apex classes referenced. -->

</PermissionSet>
